# Scratch space for intro-video intermediates (gitignored contents).

Reusable generator: `../generate-intro-video.js` (`npm run generate:intro-video`).
Outputs belong in `docs/intro/` (mp4 + poster), not here.
